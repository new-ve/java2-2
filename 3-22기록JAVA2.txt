황충은 202330234 3/22
java api 
    jdk포함 클래스 라이브러리

java package
    서로 관련된 클래스들 분류하여 묶어 놓은 것

java class
    반드시 클래스와 동일한 이름으로 파일 저장

java 특징
    플랫폼에 종속되지 않음
    가비지 컬렉션 타이밍 불확실(실시간 부적합)

식별자
    클래스, 변수, 상수, 메소드 등에 붙이는 이름
    특수 문자, 공백 또는 탭 사용 불가, '_', '$' 사용 가능
    유니코드 문자와 한글 사용 가능
    첫 번째 문자로 숫자 사용 불가
    불린, 널 리터럴 사용불가
    길이 제한 없음
    대소문자 구별됨

문자열
    기본 타입 아님
    String 클래스로 기본 문자열 표현

변수
    프로그램 실행 중에 값을 임시 저장하기 위한 공간